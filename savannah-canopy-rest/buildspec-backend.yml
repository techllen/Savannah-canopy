version: 0.2

phases:
  install:
    commands:
      - echo Installing Maven...
      - mvn --version
  build:
    commands:
      - echo Building the Spring Boot app...
      - echo listing files...
      - cd /codebuild/output/src2186584640/src/savannah-canopy-rest
      - ls
      - pwd
      - mvn clean package
      - echo listing files...
      - ls
      - pwd
artifacts:
  files:
    - target/*.jar