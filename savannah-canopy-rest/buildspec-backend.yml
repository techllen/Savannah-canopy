version: 0.2

phases:
  install:
    commands:
      - echo Installing Maven...
      - mvn --version
  build:
    commands:
      - pwd
      - ls -ltr
      - echo Building the Spring Boot app...
      - mvn clean package
artifacts:
  files:
    - target/*.jar