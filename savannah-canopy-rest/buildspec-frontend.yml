version: 0.2

phases:
  install:
    commands:
      - echo Installing Node...
      - node --version
      - npm install
  build:
    commands:
      - echo Building the React app...
      - npm run build
artifacts:
  files:
    - '**/*'
base-directory: build